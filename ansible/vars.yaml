---
# vars.yaml

# Hostnames
# hostname_server1: "postgres01"
# hostname_server2: "postgres02"

# IPs
ip_server1: 'PPP'
ip_server2: 'SSS'

# # User and Password of MySQL 
new_user: "kiausr"
new_user_password: "123"
new_db: "kiadb"
db_owner: "{{ new_user }}"
